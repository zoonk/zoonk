# Domain review checkpoint

The specialist architecture proposal in this file was consolidated into [COURSE-CONTRACTS.md](COURSE-CONTRACTS.md), which now documents implemented persistence, Core/public API shapes, concurrency rules, and current verification evidence. Use that document and the typed implementation for integration; `PROBLEMS.md` remains the source of truth. The independent original reasoning and tradeoffs remain in `COURSE-ARCHITECTURE-REVIEW.md`.

This redirect removes stale proposed names (`full`, replaced by `complete`) and preliminary allowance numbers that were never accepted as the implementation policy. All planning documents remain uncommitted.

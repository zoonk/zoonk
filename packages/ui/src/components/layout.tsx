/**
 * Wide browsing surfaces should share one max width so catalog pages, media
 * headers, and grid containers stay aligned when the product width changes.
 */
export const WIDE_CONTENT_MAX_WIDTH_CLASS = "max-w-6xl";

enum GenerationStatus {
  pending
  running
  completed
  failed
}

enum LessonKind {
  core
  language
  custom
}

enum ActivityKind {
  custom
  explanation
  quiz
  practice
  challenge
  vocabulary
  grammar
  reading
  listening
  translation
  review
  languagePractice @map("language_practice")
}

enum StepKind {
  static
  visual
  fillBlank      @map("fill_blank")
  matchColumns   @map("match_columns")
  multipleChoice @map("multiple_choice")
  selectImage    @map("select_image")
  sortOrder      @map("sort_order")
  arrangeWords   @map("arrange_words")
  vocabulary
  translation
  reading
  listening
}

enum CourseMode {
  full
  quickLesson  @map("quick_lesson")
  personalized
  examPrep     @map("exam_prep")
}

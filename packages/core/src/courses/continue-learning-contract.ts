/**
 * Keeps the Home continuation feed and its public API contract on the same
 * bounded collection size without loading server-only query dependencies.
 */
export const MAX_CONTINUE_LEARNING_ITEMS = 4;

defmodule ZoonkSchema do
  @moduledoc """
  ZoonkSchema keeps the schema definitions for
  the Zoonk application.

  Schemas are used to map the database tables
  and establish relationships between them.

  We keep all schemas listed under this module
  for easy access and organization. This allows
  us to quickly find all schemas in the application.
  """
end

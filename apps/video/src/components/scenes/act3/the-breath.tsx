import { SceneContainer } from "@/components/scene-container";

/**
 * Empty white screen. Pure silence. Creates anticipation before the close.
 * Two seconds of nothing — the inhale before the statement.
 */
export function TheBreath() {
  return <SceneContainer bg="white">{null}</SceneContainer>;
}

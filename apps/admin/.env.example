# In production, replace this with the URL for the `api` app, eg. https://api.zoonk.com
NEXT_PUBLIC_API_URL=http://localhost:3004

# In production, replace this with the URL for the `admin` app, eg. https://admin.zoonk.com
NEXT_PUBLIC_BASE_URL=http://localhost:3001

# Database connection URL for Prisma
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/zoonk
